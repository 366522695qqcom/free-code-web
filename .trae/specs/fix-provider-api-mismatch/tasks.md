# Tasks
- [x] Task 1: 修复 GET /api/providers 返回格式
  - [x] 将 `JSON.stringify(providers)` 改为 `JSON.stringify({ providers })`
- [x] Task 2: 修复 POST /api/providers 返回格式
  - [x] 将 `JSON.stringify(provider)` 改为 `JSON.stringify({ provider })`
- [x] Task 3: 修复 PUT /api/providers/[id] 返回格式
  - [x] 将 `JSON.stringify(provider)` 改为 `JSON.stringify({ provider })`
- [x] Task 4: 验证构建通过
